#include <stdio.h>
#include <unistd.h>	

int main () 
{
    int i;
    int status;
    if (fork() != 0 ){ / CREATE a child process outside of the loop that is not used
	for (i = 0; i < 2; i++) / CHANGE THE 4 to a 2 so that 3 child processes are created in the loop
	{
        	if (fork () == 0) {
            		sleep (1);		/*sleep(): unistd.h */
        	}
	}	
    	printf ("This is the end.\n");
    }
}